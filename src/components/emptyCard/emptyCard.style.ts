import { Style } from '@/types'

export const style: Style = {
  root: {},
  emptyCardStyle: {
    p: 2,
    boxShadow: 3,
    borderRadius: 2,
    alignItems: 'center',
    justifyContent: 'center',
    minHeight: 350,
  },
}
